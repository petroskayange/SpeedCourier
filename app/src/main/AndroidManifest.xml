<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.speedcourier">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="false"
        android:theme="@style/Theme.SpeedCourier"
        android:usesCleartextTraffic="true">
        <activity android:name=".UserPayments"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Payments" />
        <activity android:name=".UserProfile" />
        <activity android:name=".TrackParcel" />
        <activity android:name=".Services" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SingUpActivity" />
        <activity
            android:name=".SetUpIP"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>